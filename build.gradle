buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:${rootProject.property("android.gradle.plugin.version")}"
    }
}

allprojects {
    group = 'ru.ok'
    version = '2.0.1'

    repositories {
        mavenCentral()
    }
}
